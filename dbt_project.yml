name: ecommerce_analytics
version: 1.0.0
config-version: 2
profile: ecommerce_analytics
model-paths:
- models
analysis-paths:
- analyses
test-paths:
- tests
seed-paths:
- seeds
macro-paths:
- macros
snapshot-paths:
- snapshots
target-path: target
clean-targets:
- target
- dbt_packages
models:
  ecommerce_analytics:
    staging:
      +materialized: view
      +docs:
        show: true
    intermediate:
      +materialized: table
      +docs:
        show: true
    marts:
      +materialized: table
      +docs:
        show: true
seeds:
  ecommerce_analytics:
    +docs:
      show: true
vars:
  start_date: '2022-01-01'
  end_date: '2024-12-31'
